#pragma once

#include <map>
#include <string>

#include "battery/embed.hpp"

// This file contains auto-generated CMake battery_embed files list template.

namespace usos_rpc {

    // clang-format off

    /// @brief Contains a map of file names embedded in the source file and their content.
    const std::map<std::string, const char*> EMBEDDED_FILES {
        // @usos-rpc_EMBEDDED_FILES@
    };

    // clang-format on

}
